<!DOCTYPE html>
<html>
<head>
  <title>Azure Key Vault App</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="container">
    <h1>🔐 Azure Key Vault App</h1>
    
    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    
    <% if (secret) { %>
      <p><strong>DB Connection String:</strong></p>
      <pre><%= secret %></pre>
    <% } %>

    <hr />
    <h2>📌 Navigation</h2>
    <ul>
      <li><a href="/browse">Browse Products</a></li>
      <li><a href="/form">Submitted Messages</a></li>
      <% if (loggedIn) { %>
        <li><a href="/add-product">Add Product</a></li>
        <li><a href="/logout">Logout</a></li>
      <% } else { %>
        <li><a href="/login">Login</a></li>
      <% } %>
    </ul>
  </div>
</body>
</html>
