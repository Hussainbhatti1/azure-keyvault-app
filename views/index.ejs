<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Azure Key Vault App</title>
  <style>
    body { font-family: sans-serif; padding: 2rem; background: #f3f3f3; }
    .container { max-width: 600px; margin: auto; background: #fff; padding: 2rem; border-radius: 8px; box-shadow: 0 0 10px #ccc; }
    .error { color: #d32f2f; background: #ffebee; padding: 1rem; border-radius: 4px; }
    .environment { 
      position: absolute; 
      top: 10px; 
      right: 10px; 
      padding: 0.3rem 0.6rem; 
      border-radius: 4px; 
      font-size: 0.8rem;
      background: <%= environment === 'production' ? '#4caf50' : '#ff9800' %>;
      color: white;
    }
  </style>
</head>
<body>
  <div class="container">
    <% if (environment !== 'production') { %>
      <div class="environment">
        <%= environment.toUpperCase() %>
      </div>
    <% } %>
    
    <h1>🔐 Azure Key Vault Secret</h1>
    
    <% if (error) { %>
      <div class="error">
        <p><strong>Error:</strong> <%= error %></p>
      </div>
    <% } %>
    
    <% if (secret) { %>
      <p><strong>DB Connection String:</strong></p>
      <pre><%= secret %></pre>
    <% } else if (!error) { %>
      <p>No secret retrieved</p>
    <% } %>
  </div>
</body>
</html>